// "use strict";

// const accountModel = require("../models/Account");

// const findByUsername = async (username) => {
//   const account = await accountModel.findAll({ where: { username: username } });
//   if (!account) {
//     return "Account not found";
//   } else {
//     return account;
//   }
// };

// const findByEmail = async (email) => {
//   const account = await accountModel.findAll({ where: { email: email } });
//   if (!account) {
//     return "Account not found";
//   } else {
//     return account;
//   }
// };
// module.exports = {
//   findByEmail,
//   findByUsername,
// };
